# =============================================================================
# Cloud-Native-Ops-Starter - Service Account
# =============================================================================
# Dedicated service account for the API pods with minimal permissions.
# =============================================================================

apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloud-native-ops-api
  namespace: cloud-native-ops
  labels:
    app: cloud-native-ops-api
    app.kubernetes.io/name: cloud-native-ops-api
    app.kubernetes.io/managed-by: argocd
  annotations:
    # AWS IRSA annotation (uncomment when using External Secrets or AWS SDK)
    # eks.amazonaws.com/role-arn: arn:aws:iam::${AWS_ACCOUNT_ID}:role/cloud-native-ops-api-role
